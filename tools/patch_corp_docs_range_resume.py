from pathlib import Path

p=Path('collectors/corporate_docs_collect.py')
text=p.read_text(encoding='utf-8')
text=text.replace(
'''DOWNLOAD_ATTEMPTS = 2\nPREFLIGHT_TIMEOUT = (8, 15)\n''',
'''DOWNLOAD_ATTEMPTS = 2\nTRANSFER_ATTEMPTS = 4\nPREFLIGHT_TIMEOUT = (8, 15)\n''',1)
start=text.index('def download_one(session, doc, target, total_bytes):')
end=text.index('\n\ndef source_candidates(doc):',start)
new='''def download_one(session, doc, target, total_bytes):\n    \"\"\"Download one verified document with bounded in-run byte-range resume.\n\n    Slow official servers often terminate an otherwise valid large transfer after\n    tens of megabytes. Preserve the partial file across retryable network errors\n    and request only the remaining bytes when the origin supports HTTP Range.\n    If Range is ignored, restart safely from byte zero. The same absolute wall,\n    per-file size, and aggregate size limits still apply.\n    \"\"\"\n    url = str(doc.get(\"source_url\") or \"\")\n    headers = preflight(session, doc, url)\n    last_exc = None\n    started = time.monotonic()\n    deadline = started + BASE_DOCUMENT_WALL_SECONDS\n    active_budget = BASE_DOCUMENT_WALL_SECONDS\n    resume_offset = target.stat().st_size if target.exists() else 0\n\n    for attempt in range(1, TRANSFER_ATTEMPTS + 1):\n        remaining = deadline - time.monotonic()\n        if remaining <= 0:\n            last_exc = TimeoutError(f\"document wall-clock budget exceeded ({active_budget:.0f}s)\")\n            break\n        connect_timeout = max(1.0, min(float(DOWNLOAD_TIMEOUT[0]), remaining))\n        read_timeout = max(1.0, min(float(DOWNLOAD_TIMEOUT[1]), remaining))\n        request_headers = dict(headers)\n        if resume_offset > 0:\n            request_headers[\"Range\"] = f\"bytes={resume_offset}-\"\n        try:\n            with session.get(\n                url, stream=True, timeout=(connect_timeout, read_timeout),\n                allow_redirects=True, headers=request_headers\n            ) as r:\n                r.raise_for_status()\n                status_code = int(getattr(r, \"status_code\", 200) or 200)\n                length = int(r.headers.get(\"Content-Length\") or 0)\n                content_range = str(r.headers.get(\"Content-Range\") or \"\")\n                range_match = re.match(r\"bytes\\s+(\\d+)-(\\d+)/(\\d+|\\*)\", content_range, re.I)\n                range_accepted = bool(\n                    resume_offset > 0 and status_code == 206 and range_match\n                    and int(range_match.group(1)) == resume_offset\n                )\n\n                if resume_offset > 0 and not range_accepted:\n                    # Origin ignored or inconsistently answered Range. Never append\n                    # a full 200 response to a partial file. Restart from zero.\n                    resume_offset = 0\n                    target.unlink(missing_ok=True)\n\n                expected_total = 0\n                if range_accepted and range_match and range_match.group(3) != \"*\":\n                    expected_total = int(range_match.group(3))\n                elif length and not range_accepted:\n                    expected_total = length\n                elif length and range_accepted:\n                    expected_total = resume_offset + length\n\n                if expected_total and expected_total > MAX_FILE_BYTES:\n                    raise ValueError(f\"declared file size exceeds {MAX_FILE_BYTES} bytes\")\n                budget_basis = expected_total or max(resume_offset + length, resume_offset)\n                active_budget = wall_budget_for_length(budget_basis)\n                deadline = max(deadline, started + active_budget)\n\n                mode = \"ab\" if range_accepted else \"wb\"\n                count = resume_offset if range_accepted else 0\n                with target.open(mode) as f:\n                    for chunk in r.iter_content(1024 * 1024):\n                        if time.monotonic() > deadline:\n                            raise TimeoutError(f\"document wall-clock budget exceeded ({active_budget:.0f}s)\")\n                        if not chunk:\n                            continue\n                        count += len(chunk)\n                        if not expected_total:\n                            active_budget = wall_budget_for_length(count)\n                            deadline = max(deadline, started + active_budget)\n                        if count > MAX_FILE_BYTES or total_bytes + count > MAX_TOTAL_BYTES:\n                            raise ValueError(\"document collection size safety limit exceeded\")\n                        f.write(chunk)\n\n                if count == 0:\n                    raise ValueError(\"zero-byte document response\")\n                if expected_total and count != expected_total:\n                    raise requests.exceptions.ConnectionError(\n                        f\"incomplete document response: received={count}; expected={expected_total}\"\n                    )\n                ctype = str(r.headers.get(\"Content-Type\") or \"\").split(\";\")[0]\n                validate_payload(doc.get(\"expected_extension\"), ctype, target)\n                return r, count, ctype\n        except Exception as exc:\n            last_exc = exc\n            retryable = isinstance(exc, (requests.exceptions.RequestException, TimeoutError))\n            if not retryable:\n                target.unlink(missing_ok=True)\n                break\n            resume_offset = target.stat().st_size if target.exists() else 0\n            if attempt < TRANSFER_ATTEMPTS and time.monotonic() < deadline:\n                time.sleep(min(0.5 * attempt, max(0.0, deadline - time.monotonic())))\n                continue\n            break\n    raise last_exc\n'''
text=text[:start]+new+text[end:]
p.write_text(text,encoding='utf-8')

p=Path('tests/test_corporate_docs_collect.py')
t=p.read_text(encoding='utf-8')
t=t.replace(
'from corporate_docs_collect import DOWNLOAD_ATTEMPTS, DOWNLOAD_TIMEOUT, PREFLIGHT_TIMEOUT, ATTACHMENT_DISCOVERY_TIMEOUT, BASE_DOCUMENT_WALL_SECONDS, MAX_DOCUMENT_WALL_SECONDS, wall_budget_for_length, collect',
'from corporate_docs_collect import DOWNLOAD_ATTEMPTS, TRANSFER_ATTEMPTS, DOWNLOAD_TIMEOUT, PREFLIGHT_TIMEOUT, ATTACHMENT_DISCOVERY_TIMEOUT, BASE_DOCUMENT_WALL_SECONDS, MAX_DOCUMENT_WALL_SECONDS, wall_budget_for_length, download_one, collect',1)
t=t.replace(
'''        self.assertLessEqual(DOWNLOAD_ATTEMPTS,2)\n        self.assertLessEqual(PREFLIGHT_TIMEOUT[1],15)\n''',
'''        self.assertLessEqual(DOWNLOAD_ATTEMPTS,2)\n        self.assertLessEqual(TRANSFER_ATTEMPTS,4)\n        self.assertLessEqual(PREFLIGHT_TIMEOUT[1],15)\n''',1)
t=t.replace('''            self.assertEqual(session.get.call_count,DOWNLOAD_ATTEMPTS)\n            rows=list(csv.DictReader((out/\"document_index.csv\").open(encoding=\"utf-8-sig\")))\n            self.assertEqual(rows[0][\"collection_status\"],\"DOWNLOAD_FAILED\")\n            self.assertIn(\"expected PDF payload\",rows[0][\"notes\"])\n''','''            self.assertEqual(session.get.call_count,1)\n            rows=list(csv.DictReader((out/\"document_index.csv\").open(encoding=\"utf-8-sig\")))\n            self.assertEqual(rows[0][\"collection_status\"],\"DOWNLOAD_FAILED\")\n            self.assertIn(\"expected PDF payload\",rows[0][\"notes\"])\n''',1)
t=t.replace('''            session=unittest.mock.MagicMock(); session.get.side_effect=[blocked,blocked,source_page,fallback]\n''','''            session=unittest.mock.MagicMock(); session.get.side_effect=[blocked,source_page,fallback]\n''',1)
t=t.replace('''            session=unittest.mock.MagicMock(); session.get.side_effect=[blocked,blocked]\n''','''            session=unittest.mock.MagicMock(); session.get.side_effect=[blocked]\n''',1)
t=t.replace('''            self.assertEqual(session.get.call_count,DOWNLOAD_ATTEMPTS)\n            attempts=list(csv.DictReader((out/\"download_attempts.csv\").open(encoding=\"utf-8-sig\")))\n''','''            self.assertEqual(session.get.call_count,1)\n            attempts=list(csv.DictReader((out/\"download_attempts.csv\").open(encoding=\"utf-8-sig\")))\n''',1)
marker='''    def test_request_scope_mismatch_fails_closed_without_network(self):\n'''
case='''    def test_interrupted_transfer_resumes_with_http_range(self):\n        import requests\n        with tempfile.TemporaryDirectory() as td:\n            target=Path(td)/\"resume.pdf\"\n            full=b\"%PDF-abcdefghijklmno\"\n            cut=8\n\n            class Interrupted(FakeResponse):\n                status_code=200\n                def __init__(self):\n                    super().__init__(body=full, url=\"https://official.example/report.pdf\")\n                    self.headers[\"Content-Length\"]=str(len(full))\n                def iter_content(self,size):\n                    yield full[:cut]\n                    raise requests.exceptions.ConnectionError(\"stream interrupted\")\n\n            class Resumed(FakeResponse):\n                status_code=206\n                def __init__(self):\n                    super().__init__(body=full[cut:], url=\"https://official.example/report.pdf\")\n                    self.headers[\"Content-Length\"]=str(len(full)-cut)\n                    self.headers[\"Content-Range\"]=f\"bytes {cut}-{len(full)-1}/{len(full)}\"\n\n            session=unittest.mock.MagicMock(); session.get.side_effect=[Interrupted(),Resumed()]\n            with patch(\"corporate_docs_collect.time.sleep\"):\n                _,count,ctype=download_one(session,{\"source_url\":\"https://official.example/report.pdf\",\"expected_extension\":\"pdf\"},target,0)\n            self.assertEqual(count,len(full))\n            self.assertEqual(target.read_bytes(),full)\n            self.assertEqual(ctype,\"application/pdf\")\n            self.assertEqual(session.get.call_args_list[1].kwargs[\"headers\"][\"Range\"],f\"bytes={cut}-\")\n\n    def test_range_ignored_restarts_without_corrupt_append(self):\n        import requests\n        with tempfile.TemporaryDirectory() as td:\n            target=Path(td)/\"restart.pdf\"\n            full=b\"%PDF-restart-complete\"\n            cut=7\n\n            class Interrupted(FakeResponse):\n                status_code=200\n                def __init__(self):\n                    super().__init__(body=full, url=\"https://official.example/report.pdf\")\n                    self.headers[\"Content-Length\"]=str(len(full))\n                def iter_content(self,size):\n                    yield full[:cut]\n                    raise requests.exceptions.ConnectionError(\"stream interrupted\")\n\n            second=FakeResponse(body=full,url=\"https://official.example/report.pdf\")\n            second.status_code=200\n            session=unittest.mock.MagicMock(); session.get.side_effect=[Interrupted(),second]\n            with patch(\"corporate_docs_collect.time.sleep\"):\n                _,count,_=download_one(session,{\"source_url\":\"https://official.example/report.pdf\",\"expected_extension\":\"pdf\"},target,0)\n            self.assertEqual(count,len(full))\n            self.assertEqual(target.read_bytes(),full)\n            self.assertIn(\"Range\",session.get.call_args_list[1].kwargs[\"headers\"])\n\n'''
assert marker in t, 'test insertion marker not found'
t=t.replace(marker,case+marker,1)
p.write_text(t,encoding='utf-8')
